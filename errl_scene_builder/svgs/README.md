Asset workspace for Errl Scene Synth SVGs.

- Artboard: 1024x1024, origin top-left.
- Style: inline `fill`/`stroke`, minimal groups/paths, no external styles.
- Palette (starting point): `#101020` base, `#f4f0ff` highlight, `#5be0ff` glow, `#ff7ad1` accent, `#ffd166` warm accent, `#1cdd9c` secondary.
- Stroke hint: 8px default on 1024 artboard; round joins/caps where applicable.
- Group naming: lowercase with underscores (e.g., `group_head_base`, `group_glow_cone`); z-depth suffix `_FG/_MG/_BG` when relevant.
